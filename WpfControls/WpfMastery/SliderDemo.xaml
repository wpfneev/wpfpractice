<Window x:Class="WpfMastery.SliderDemo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfMastery"
        mc:Ignorable="d"
        Title="SliderDemo" Height="400" Width="400">
    <StackPanel VerticalAlignment="Center" Margin="20">
        <Slider 
            VerticalAlignment="Center" 
            Maximum="100"
            x:Name="mySlider"
            TickPlacement="BottomRight"
            TickFrequency="10"
            IsSnapToTickEnabled="True"
            Value="50"
            Minimum="10"
            ValueChanged="mySlider_ValueChanged"
            >
        </Slider>
        <TextBlock x:Name="myTextBlock"
                   FontSize="{Binding ElementName=mySlider, Path=Value, UpdateSourceTrigger=PropertyChanged}">
            Hello, I am TextBoxBlock
        </TextBlock>
    </StackPanel>
</Window>
